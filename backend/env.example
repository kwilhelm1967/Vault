# =============================================================================
# Local Password Vault - Backend Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3001

# =============================================================================
# DATABASE
# =============================================================================
# SQLite database file path (default: ./database/vault.db)
DATABASE_PATH=./database/vault.db

# =============================================================================
# JWT SECRET
# =============================================================================
# Used to sign license tokens for offline validation
# Generate a strong random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# STRIPE CONFIGURATION
# =============================================================================
# Get these from https://dashboard.stripe.com/apikeys

# Secret Key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Webhook Secret (starts with whsec_)
# Get this when you create a webhook endpoint in Stripe Dashboard
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Price IDs (optional - if not set, prices are created on the fly)
# Create products in Stripe Dashboard and copy the price IDs
STRIPE_PRICE_PERSONAL=price_xxx
STRIPE_PRICE_FAMILY=price_xxx

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# Choose your email provider: sendgrid, mailgun, or smtp
EMAIL_PROVIDER=smtp

# --- SendGrid ---
SENDGRID_API_KEY=SG.your_sendgrid_api_key

# --- Mailgun ---
MAILGUN_USER=postmaster@your-domain.mailgun.org
MAILGUN_PASSWORD=your_mailgun_password

# --- SMTP (Gmail, custom SMTP, etc.) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email sender info
FROM_EMAIL=noreply@localpasswordvault.com
SUPPORT_EMAIL=support@localpasswordvault.com

# =============================================================================
# WEBSITE URLs
# =============================================================================
# Your main website URL (no trailing slash)
WEBSITE_URL=https://localpasswordvault.com

# =============================================================================
# CORS ORIGINS
# =============================================================================
# Comma-separated list of allowed origins (in addition to defaults)
# CORS_ORIGINS=https://app.localpasswordvault.com,https://staging.localpasswordvault.com

